# Contracts

This feature is local-first and does not expose backend APIs.

- No external services.
- Import/Export: JSON file containing arrays of `Task` and `Preferences`.
- Future-proofing: If sync is added later, define versioned schemas and conflict rules.
